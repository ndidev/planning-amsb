/**
 * URLs de l'application.
 */
export type AppURLs = {
  /**
   * Objet `URL` de l'API générale.
   */
  api: URL;

  /**
   * Objet `URL` de l'API d'authentification.
   */
  auth: URL;

  /**
   * Objet `URL` du serveur Server-Sent Events.
   */
  sse: URL;

  /**
   * Objet `URL` des logos.
   */
  logos: URL;
};
