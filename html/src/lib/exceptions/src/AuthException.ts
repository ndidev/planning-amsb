/**
 * Erreur lancée lorsque l'authentification n'est pas valide.
 */
export class AuthException extends Error {
  /**
   * Erreur lancée lorsque l'authentification n'est pas valide.
   */
  constructor(response: Response) {
    super();
  }
}
